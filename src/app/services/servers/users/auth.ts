"use server";

export const proceedLogin = async (data: LoginFormData) => {
    try {
        console.log("🚀 ~ proceedLogin ~ error:", "frank fontcha", data)
        return "tokoss";
    } catch (error) {
        console.log("🚀 ~ proceedLogin ~ error:", error)
    }
}